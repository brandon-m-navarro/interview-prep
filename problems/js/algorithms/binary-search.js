/**
 * Binary Serach
 *  
 * Given an array of integers searchArr which is sorted in ascending order,
 * and an integer target, write a function to search target in searchArr. If
 * target exists, then return its index. Otherwise, return -1.
 *
 * The algorithm should be O(log n).
 * 
 * Constraints:
 *   0 <= searchArr.length <= 10^4
 *   -10^4 < searchArr[i], target < 10^4
 *   All the integers in searchArr are unique.
 *   searchArr is sorted in ascending order.
 */
export default function binarySearch(searchArr, target) {}
